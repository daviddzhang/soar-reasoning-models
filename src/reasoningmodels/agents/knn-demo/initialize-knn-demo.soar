sp {propose*initialize-knn-demo
   (state <s> ^superstate nil
             -^name)
-->
   (<s> ^operator <o> +)
   (<o> ^name initialize-knn-demo)
}

sp {apply*initialize-knn-demo
   (state <s> ^operator <op>)
   (<op> ^name initialize-knn-demo)
-->
   (<s> ^name knn-demo)
}

