sp {propose*initialize-bn-demo
   (state <s> ^superstate nil
             -^name)
-->
   (<s> ^operator <o> +)
   (<o> ^name initialize-bn-demo)
}

sp {apply*initialize-bn-demo
   (state <s> ^operator <op>)
   (<op> ^name initialize-bn-demo)
-->
   (<s> ^name bn-demo)
}

